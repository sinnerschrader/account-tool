FROM openjdk:jre-alpine

ADD docker/entrypoint.sh \
    build/libs/account-tool-*.jar \
    /app/

WORKDIR /app

ENV SPRING_PROFILES_ACTIVE=overrides 

ENTRYPOINT [ "/app/entrypoint.sh" ]
CMD java -Xss64m -Xmx64m -jar account-tool-*.jar
